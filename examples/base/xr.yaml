apiVersion: base.example.com/v1
kind: XBaseInfrastructure
metadata:
  name: platform-base
  labels:
    app: myplatform
    environment: staging
spec:
  region: us-west-2
  vpcId: vpc-12345678
  dbUsername: admin
  sshPublicKey: qa_ssh_key
  team: devops
